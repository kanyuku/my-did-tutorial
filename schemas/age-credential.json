{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Age Credential Schema",
  "description": "Schema for verifying a person's age without revealing exact birthdate.",
  "type": "object",
  "properties": {
    "dateOfBirth": {
      "type": "string",
      "format": "date",
      "description": "Holder's date of birth in YYYY-MM-DD format"
    },
    "ageThreshold": {
      "type": "integer",
      "minimum": 0,
      "description": "The age threshold being verified (e.g., 18)"
    }
  },
  "required": ["dateOfBirth", "ageThreshold"]
}
